# Unified VRDS Platform Environment Configuration

# Server Configuration
PORT=4000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/wildfire_study

# Production MongoDB (comment out for local dev)
# MONGODB_URI=mongodb://username:password@host:port/wildfire_study?authSource=admin

# CORS Configuration (for production)
# Automatically configured based on NODE_ENV
# Development: http://localhost:5173, http://localhost:5174, etc.
# Production: https://moonlander.fit.edu, http://moonlander.fit.edu

# Experiment Configuration
# Active experiments (comma-separated)
ACTIVE_EXPERIMENTS=CVR_APA,CVR_ONLY

# Future experiments (currently inactive)
# ACTIVE_EXPERIMENTS=CVR_APA,CVR_ONLY,APA_ONLY,BASELINE
